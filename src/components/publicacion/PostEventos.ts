import { Evento } from "./Evento";

export class PostEventos {
    public crearEvento(evento: Evento): void {
      // Lógica para crear un evento
    }
  
    public actualizarEvento(evento: Evento): void {
      // Lógica para actualizar un evento
    }
  
    public eliminarEvento(evento: Evento): void {
      // Lógica para eliminar un evento
    }
  
    public listarEventos(): Evento[] {
      // Lógica para obtener la lista de eventos
      return [];
    }
  }
  